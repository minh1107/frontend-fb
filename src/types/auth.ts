export type RegisterType = {
    first_name: string,
    last_name: string,
    email: string,
    password: string,
    rePassword: string
}

export type LoginType = {
    email: string,
    password: string
}